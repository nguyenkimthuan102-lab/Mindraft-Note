// Cảnh báo: File này đang để trống nên gây ra lỗi "missing default export" trên Expo.
// Tạm thời xuất ra một component rỗng để fix lỗi. Nếu route này không còn được sử dụng,
// có thể xóa hoàn toàn thư mục `app/note`.

export default function NoteRoute() {
  return null;
}
